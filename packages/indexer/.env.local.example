# Copied from the repo root .env by `pnpm dev`; kept here so `ponder dev` can be
# run from this package on its own.
RHC_RPC_URL=https://rpc.mainnet.chain.robinhood.com
RHC_RPC_RPS=3
EXDATE_POLL_INTERVAL_BLOCKS=600
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/exdate
