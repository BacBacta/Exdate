node_modules/
dist/
build/
.next/
out/
coverage/
.turbo/
*.tsbuildinfo

.env
.env.*
!.env.example
!*.example

.ponder/
# Runtime state the indexer owns: the delivery journal and the self-service subscriptions.
# On the machine these live on a volume; here they are whatever a test or a local run produced,
# and one of them was committed once as if it held real deliveries.
.exdate/
*.log
.DS_Store
.vscode/
.idea/

.vercel/
.vercel
.env*

# copied from data/ before every site build
apps/web/public/data/
